# Step 1: Select records from TraceSession having status : Not processed and Not proces_error.
# Step 2: Call get session API and update status on basis of session_id.
# Step 3: If status is processed then, Update status and call session result API and update result.
